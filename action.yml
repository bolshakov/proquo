name: Review price tag
description: Comments the estimated review burden in reviewer minutes on every pull request
inputs:
  github-token:
    description: Token used to read PR files and write the sticky comment
    required: true
runs:
  using: node20
  main: dist/index.mjs
